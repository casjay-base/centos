[ftp]
  path = /var/ftp/pub
  comment = FTP server
  # Deliberately anonymous - this mirrors the public /var/ftp/pub tree that
  # proftpd already serves; it is read only and never runs as root
  uid = ftp
  gid = ftp
  read only = yes
  list = yes
