# run security updates - runs daily at 2 am
# No ICMP connectivity gate: dnf fails on its own when the repos are
# unreachable, and ping is blocked on plenty of otherwise-online networks
0 2 * * * root dnf -y --security upgrade >/dev/null 2>&1
